# d-ssvm example: plot decision boundary

This example shows a binary classification result using linear subgradient SVM of 2D synthetic data.

![plot](plot.png)

``` console
$ dub run --compiler=dmd --build=release-nobounds
$ open plot.png
```
